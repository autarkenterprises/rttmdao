# Copy to .env in this directory (not committed):
#   cp infra/gcp/.env.example infra/gcp/.env
#
export POOL_ADDRESS=0xYourRttmPoolOnSepolia
# Circle USDC on Sepolia is the usual treasury for this stack; RPC default matches apps/web.
export RPC_URL=https://rpc.sepolia.org
# export CHAIN_ID=11155111
# export FROM_BLOCK=0
# export VM_NAME=rttmdao-indexer
# export GCP_ZONE=us-central1-a
